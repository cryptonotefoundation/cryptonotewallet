#include "Hash.h"

void Hash::hash(const QString &data, const QString &password){
    //SHA1CryptoServiceProvider sha = new SHA1CryptoServiceProvider();
    //return sha.ComputeHash(System.Text.Encoding.ASCII.GetBytes(userID + password));
}
